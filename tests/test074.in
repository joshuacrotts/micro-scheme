(define l '(+ * / - (+ * / -) (- * + /) - - - -))
(define (cddr l) (cdr (cdr l)))
(define (cdddr l) (cdr (cddr l)))
(define (cddddr l) (cdr (cdddr l)))
(car l)
(cdr l)
(cddr l)
(cdddr l)
(cddddr l)
(car (cddddr l))
(eq? (car (car (cddddr l))) '+)
